<!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Facturas - Listado</title>
</head>
<body class="bg-gray-50">
  <div class="max-w-4xl mx-auto p-4">
    <h1 class="text-2xl font-bold mb-4">Gestor de Facturas (MVP)</h1>

    <!-- Bloque CV -->
    <section class="mb-6 bg-white shadow rounded p-4 text-center">
      <h2 class="text-xl font-semibold">Leandro Maciel — Asistente Virtual · Soporte al Cliente · Data Entry · Contenido</h2>
      <p class="text-gray-700 mt-1">Docente de Geografía y Ciencias Sociales. Organización, comunicación clara y foco en servicio al usuario.</p>

      <div class="flex flex-wrap gap-2 justify-center mt-3 text-sm">
        <span class="px-2 py-1 border rounded">Google Workspace</span>
        <span class="px-2 py-1 border rounded">Sheets/Excel</span>
        <span class="px-2 py-1 border rounded">Notion</span>
        <span class="px-2 py-1 border rounded">Trello</span>
        <span class="px-2 py-1 border rounded">Slack</span>
        <span class="px-2 py-1 border rounded">Zoom</span>
      </div>

      <div class="mt-3 flex gap-3 justify-center flex-wrap">
        <a class="px-3 py-2 rounded bg-blue-600 text-white" href="mailto:leandrotvr@gmail.com" target="_blank" rel="noopener">Contactar</a>
        <a class="px-3 py-2 rounded border" href="https://leandrotvr.github.io" target="_blank" rel="noopener">Portafolio</a>
        <a class="px-3 py-2 rounded border" href="https://github.com/Leandrotvr" target="_blank" rel="noopener">GitHub</a>
        <a class="px-3 py-2 rounded border" href="https://mi-to-do-list.onrender.com" target="_blank" rel="noopener">To Do List (Fullstack)</a>
        <a class="px-3 py-2 rounded border" href="https://foro-front.onrender.com/" target="_blank" rel="noopener">Foro Fullstack</a>
      </div>

      <p class="mt-3 text-xs text-gray-500">Idiomas: Español (nativo) · Inglés (intermedio) · Italiano (A1)</p>
    </section>

    <!-- Buscador + acciones -->
    <form class="flex gap-2 mb-4" method="get" action="/">
      <input name="q" value="<%= q %>" placeholder="Buscar por número o cliente" class="border p-2 flex-1"/>
      <button class="bg-blue-600 text-white px-4 py-2 rounded">Buscar</button>
      <a href="/facturas/nueva" class="bg-green-600 text-white px-4 py-2 rounded">Nueva factura</a>
      <a href="/clientes/nuevo" class="px-4 py-2 rounded border">Nuevo cliente</a>
    </form>

    <!-- Listado de facturas -->
    <table class="w-full bg-white shadow rounded">
      <thead><tr class="bg-gray-100">
        <th class="p-2 text-left">#</th>
        <th class="p-2 text-left">Cliente</th>
        <th class="p-2 text-left">Fecha</th>
        <th class="p-2 text-left">Año</th>
        <th class="p-2 text-right">Total</th>
        <th class="p-2">Acciones</th>
      </tr></thead>
      <tbody>
      <% (facturas || []).forEach(function(f){ %>
        <tr class="border-t">
          <td class="p-2"><%= f.numero %></td>
          <td class="p-2"><%= f.cliente %></td>
          <td class="p-2"><%= f.fecha %></td>
          <td class="p-2"><%= f.año %></td>
          <td class="p-2 text-right">$ <%= f.total %></td>
          <td class="p-2 text-center">
            <a class="text-blue-700 underline" href="/facturas/<%= f.id %>">Ver</a>
          </td>
        </tr>
      <% }); %>
      </tbody>
    </table>

    <%- include('partials/footer') %>
  </div>
</body>
</html>
