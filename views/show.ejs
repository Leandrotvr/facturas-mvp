<!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Factura</title>
</head>
<body class="bg-gray-50">
<div class="max-w-3xl mx-auto p-4">
  <div class="bg-white shadow rounded p-4">
    <div class="flex justify-between items-center">
      <h2 class="text-xl font-semibold">Factura <%= f.numero %></h2>
      <div class="flex gap-2">
        <a class="bg-gray-700 text-white px-3 py-1 rounded" href="/facturas/<%= f.id %>/pdf">Descargar PDF</a>
        <form method="post" action="/facturas/<%= f.id %>/borrar" onsubmit="return confirm('¿Borrar?')">
          <button class="bg-red-600 text-white px-3 py-1 rounded">Borrar</button>
        </form>
      </div>
    </div>
    <p class="text-sm text-gray-600">Fecha: <%= dayjs(f.fecha).format('DD/MM/YYYY') %> — Año: <%= f.año %></p>
    <p class="mt-1">Cliente: <strong><%= f.cliente %></strong> | CUIT: <%= f.cuit || '-' %> | Email: <%= f.email || '-' %></p>

    <h3 class="mt-4 font-semibold">Items</h3>
    <table class="w-full">
      <thead><tr class="text-left border-b">
        <th class="p-2">Descripción</th><th class="p-2">Cant.</th>
        <th class="p-2">Precio</th><th class="p-2">Monto</th>
      </tr></thead>
      <tbody>
        <% f.items.forEach(it=>{ %>
          <tr class="border-b">
            <td class="p-2"><%= it.descripcion %></td>
            <td class="p-2"><%= it.cantidad %></td>
            <td class="p-2">$ <%= it.precio_unitario %></td>
            <td class="p-2">$ <%= it.monto %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>

    <div class="text-right mt-4 space-y-1">
      <p>Subtotal: $ <%= f.subtotal %></p>
      <p>Impuesto: $ <%= f.impuesto %></p>
      <p class="font-bold text-lg">TOTAL: $ <%= f.total %></p>
    </div>
    <div class="mt-4">
      <a href="/" class="text-blue-700 underline">Volver al listado</a>
    </div>
  </div>
</div>
</body>
</html>
